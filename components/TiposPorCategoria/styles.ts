import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        paddingRight: 10,
        gap: 5
    },
    button: {
        padding: 5,
        marginTop: 10,
        width: 100,
        borderRadius: 25,
        alignItems: 'center'
    }
})
